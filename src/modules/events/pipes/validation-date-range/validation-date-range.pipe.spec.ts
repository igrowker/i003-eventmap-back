import { ValidationDateRangePipe } from './validation-date-range.pipe';

describe('ValidationDateRangePipe', () => {
  it('should be defined', () => {
    expect(new ValidationDateRangePipe()).toBeDefined();
  });
});
